<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android"> <!-- Cor quando pressionado -->
    <item android:color="@android:color/transparent" android:state_pressed="true" /> <!-- Cor quando selecionado -->
    <item android:color="@android:color/transparent" android:state_checked="true" /> <!-- Transparente quando não pressionado/selecionado -->
    <item android:color="@android:color/transparent" />
</selector>